'use client'

import CreateLandingPage from "@/components/createHero";
import CreateLink from "@/components/createLink";

export default function HomePage() {
    return (
        <main>
            <CreateLandingPage />
            <CreateLink />
        </main>
    )
}